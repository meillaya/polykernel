# pass2 F3 - REAL MANUAL QA (agent-executed)

- **Plan:** `.omo/plans/polykernel-pass2.md` (final verification wave, F3)
- **Verifier:** Sisyphus-Junior (unspecified-high), 2026-08-15
- **Method:** every runnable path actually executed with captured output; pod workloads
  NOT re-run (bounded spend) - the committed evidence under `reports/` was re-verified for
  authenticity via read-only SSH to the pod (files exist + non-empty + content checks).
- **Local machine:** x86_64-linux, nix develop shell (llvmPackages_21 MLIR / cudaPackages_12_6 / rocmPackages.clr)
- **Pod:** `ubuntu@216.81.200.13:22` (`private_key.pem`, mode 600) - RTX 6000 Ada (sm_89)

---

## 1. Local gate after the devenv bump (v2.2.2)

Command: `nix develop --impure --accept-flake-config -c bash -c '...'`

```
Running           devenv:enterShell
Succeeded         devenv:enterShell (6.43ms)
1 Skipped, 2 Succeeded
PolyKernel dev shell (llvmPackages_21 MLIR / cudaPackages_12_6 / rocmPackages.clr)
  nvcc:        Cuda compilation tools, release 12.6, V12.6.85
  hipcc:       HIP version: 7.2.53211-9999
  cmake:       cmake version 4.1.2
  clang:       clang version 21.1.8
numpy 2.5.0 | ml_dtypes 0.5.4
```

Build: `cmake --build build` -> `ninja: no work to do.` (exit 0)

### 1a. lit suite
Command: `cmake --build build --target check-polykernel`

```
Total Discovered Tests: 14
  Passed: 14 (100.00%)
```
**lit 14/14 PASS** (exit 0).

### 1b. ctest
Command: `ctest --test-dir build --output-on-failure`

```
157/158 Test #157: kernel_cache_persist.LoadPersistedRejectsMalformedBinaries ...   Passed    0.03 sec
158/158 Test #158: kernel_cache_persist.HashFileMissingFileIsError ..............   Passed    0.03 sec

100% tests passed, 0 tests failed out of 158
```
**ctest 158/158 PASS** (0 failed; 154 baseline + the 4 new hand-computed sm_89 fixtures).

## 2. nvcc_driver: CUDA MMA kernel compile + PTX + 0 spills

Command:
`python3 tools/polykernel-bench/nvcc_driver.py --kernel matmul_mma --arch sm_80,sm_89,sm_90 --ptx` (exit 0)

```
archs:  sm_80, sm_89, sm_90
--- ptxas -v (sm_80) ---
  [parsed] registers=40 smem=2048B spill_stores=0B spill_loads=0B stack=0B cmem0=388B
--- ptxas -v (sm_89) ---
  [parsed] registers=48 smem=2048B spill_stores=0B spill_loads=0B stack=0B cmem0=388B
--- ptxas -v (sm_90) ---
  [parsed] registers=48 smem=2048B spill_stores=0B spill_loads=0B stack=0B cmem0=0B
OK: compiled matmul_mma for sm_80, sm_89, sm_90; PTX emitted.
```

Real tensor-core instruction present in the emitted PTX:
`grep -c "mma.sync.aligned.row.col.m16n16k16.f32.bf16.bf16.f32" build/nvcc/matmul_mma_sm_89.ptx` -> `2` (`MMA_INSTRUCTION_PRESENT`).

**0 spill stores / 0 spill loads on all 3 archs. PASS.** (The committed todo-7 evidence
`reports/pass2_cuda_run_build.log` extends the 0-spill assertion to all 6 scalar kernels x
3 archs: 36 `spill_stores=0B spill_loads=0B` parsed assertions.)

## 3. HIP regression on the local RX 7800 XT (gfx1101)

Command:
`python3 -m pytest tests/kernels/test_hip_run.py tests/kernels/test_wmma.py tests/kernels/test_attention.py tests/kernels/test_quant.py -q`

```
.................................................                        [100%]
49 passed in 39.37s
```
**49/49 PASS on the 7800 XT** - no HIP regression from the pass-2 shared-template fixes
(pk_store_bf16x2 materialization + gelu_one double-erf chain).

## 4. Dashboard: MEASURED sm_89 section + PROJECTED labels

Committed dashboard `reports/benchmark_report.md` (generated by
`tools/polykernel-report/dashboard.py --dashboard`, 2026-08-15T15:17:29Z):

```
## CUDA speedups (unfused = 1.00x baseline)

> NVIDIA H100 (SXM5, sm_90): PROJECTED; NVIDIA A100 (80GB, sm_80): PROJECTED; NVIDIA RTX 6000 Ada (sm_89): MEASURED

| arch | backend | status | unfused | fused | autotuned |
| NVIDIA H100 (SXM5, sm_90) | cuda (sm_90) | PROJECTED | 1.000x | 1.004x | 2.838x |
| NVIDIA A100 (80GB, sm_80) | cuda (sm_80) | PROJECTED | 1.000x | 1.003x | 2.862x |
| NVIDIA RTX 6000 Ada (sm_89) | cuda (sm_89) | MEASURED | 1.000x | 0.838x | 0.857x |
- MEASURED (CUDA events): NVIDIA RTX 6000 Ada (sm_89): unfused 25.5181 ms / fused 30.4589 ms / autotuned 29.7851 ms
> NOTE: ... fused speedup 0.838x < 1.0x - ... MEASURED as-is, not projected.
...
- **failed correctness: 0**
```

Grep verification:
- `grep "MEASURED" reports/benchmark_report.md` -> the sm_89 row + section status line + wall-time line (above).
- `grep "PROJECTED"` -> H100/A100/MI300 rows all labeled PROJECTED; caveats state
  "H100/A100/MI300 speedups are PROJECTED ... The RTX 6000 Ada (sm_89) row is MEASURED".
- `failed correctness: 0` present twice (correctness section + compiler stats).
- No SOTA claim: `grep -in "beat vLLM"` -> only the 4 disclaimer lines.

**PASS.** The honest 0.838x fusion result (SLOWER at this compute-bound shape) is displayed
with the shape-dependence NOTE, not hidden or massaged.

## 5. Docs audit sweeps

| sweep | command | result |
|---|---|---|
| stale "no NVIDIA GPU" claims | `grep -rn "no NVIDIA GPU" docs/ README.md` | 3 hits, ALL nuanced/true: "the local dev machine has no NVIDIA GPU" (remote pod is the target) - no false claim |
| stale "runtime-validated on rental" | `grep -rn "runtime-validated on rental" docs/ README.md` | 0 hits (claim removed everywhere) |
| stale lit count "13 tests" | `grep -rn "13 tests" docs/ README.md` | 0 hits (14 everywhere) |
| SOTA/vLLM-beating claims | `grep -rn -i "beat vLLM\|beating vLLM" docs/ README.md reports/benchmark_report.md` | 4 hits, all disclaimers ("Is not: a claim to beat vLLM...") |
| leaked key material | `git grep "BEGIN.*PRIVATE KEY"` | exit 1, nothing (only lowercase "private key" mentions in pod logs describing the mode-600 hygiene check) |

**PASS.**

## 6. Pod evidence: authenticity re-verification (READ-ONLY - workloads NOT re-run)

SSH: `ssh -i private_key.pem ubuntu@216.81.200.13` (ConnectTimeout=15, key mode 600).

```
GPU 0: NVIDIA RTX 6000 Ada Generation (UUID: GPU-5c10936b-d2ef-7838-45ce-354ccb8aed39)
```
Same device UUID as `reports/pod_env_v2.log` CHECK 2 - the same instance that produced the
evidence. All six evidence files exist + non-empty on the pod at `~/polykernel/reports/`:

| file | pod size | md5 vs committed copy |
|---|---|---|
| pass2_ada_cuda_run.log | 2654 B | content-identical except pytest wall-clock + appended annotation (see below) |
| pass2_ada_cuda_run.json | 5961 B | identical except `generated_at` timestamp |
| pass2_ada_mma.log | 2466 B | **byte-identical** |
| ada6000_bench.json | 3270 B | **byte-identical** |
| pass2_ada_autotune.log | 2971 B | **byte-identical** |
| pod_env_v2.log | 2676 B | **byte-identical** |

### 6a. test_cuda_run.py on the pod -> 0 failed correctness (`pass2_ada_cuda_run.log` + `.json`)
- `18 passed in 15.22s`; dev probe `DEVICE NVIDIA RTX 6000 Ada Generation COMPUTE_CAPABILITY 8.9`; exit 0.
- Harness metrics JSON: **17/17 passed, 0 failed**, `cosine_min=1.0`, `bit_exact_min=0.999992`
  (all 17 per-test `cosine==pcc==1.0`; the single <1.0 bit_exact is `rmsnorm_with_weight`
  at 0.999992 = 99.9992% with rel 6.135e-3 < the 1e-2 ceiling - documented class).
- Verdict line: `18/18 passed - 0 failed correctness - cosine==pcc==1.0, >= 99.99% bit-exact on the RTX 6000 Ada (sm_89)`.

### 6b. test_mma.py on the pod -> 0 failed correctness (`pass2_ada_mma.log`)
- **5 passed in 5.08s**; all 4 golden shapes `path=mma validated=true` (cosine 1.0, pcc ~1.0,
  bit_exact 100%/99.997%/100%; large-K 99.986% = the documented reduction-order class);
- Negative arm on the GPU: `mma_bad validated=False` (cosine 0.0475 -> EXCLUDED) while the
  scalar baseline STANDS (cosine=pcc=1.0, bit_exact 100%) - MMA is safely additive;
- Device annotation: `NVIDIA RTX 6000 Ada Generation, 8.9, 580.126.09` + `[polykernel-mma] path=mma M=16 N=16 K=16`.

### 6c. bench_cuda.cpp on the pod -> real numbers (`ada6000_bench.json`)
- `"measured": true`, `"arch": "sm_89"`, `"device": "NVIDIA RTX 6000 Ada Generation"`,
  shape M=2048 N=11008 K=4096 bf16, CUDA events (warmup=5, iters=20, tune_iters=50).
- Real ms: unfused median **25.51808** / fused median **30.45888** / autotuned min **29.78509**
  (autotuned median 30.12710); speedups **1.0 / 0.8378 / 0.8567**.
- `archs.ada6000` block carries the same numbers for the dashboard row; caveats state the
  fused-slower cause (per-tile RMS recompute at AI~1215 >> ridge) and the scalar-attainment note.

### 6d. autotuner on the pod -> validated + broken rejected (`pass2_ada_autotune.log`)
- `bench.py --backend cuda --arch sm_89 --autotune --op fused_matmul_bias_gelu --shape 2048,4096,11008 --dtype bf16 --variants 8 --inject-broken`:
  **8/8 configs VALIDATED** with real CUDA-event ms (min 27.04282 / median 27.04896..27.39610);
  **`broken_fast` REJECTED** (`cosine=0.564039 rel=7.0e+06 pcc=0.0 -> validated:false, NOT timed, excluded`);
  BEST config_0 median 27.04896 ms.
- Cache entry: `gpu:"sm_89"`, `scored_by:"measure"`, `validated:true`, `time_ms:27.04896`,
  correctness cosine>=0.999/pcc>=0.99 - real `scored_by=measure` sm_89 entries (contract H).
- Consumption check (`reports/pass2_ada_autotune_consume.log`): `KernelCache::LoadTuningCache`
  + `Select("sm_89", ...)` returns the written best entry byte-matching; `Select("sm_90", ...)`
  is a clean miss (no cross-arch leak).

### 6e. byte-identity notes on the two non-identical files (benign, verified)
- `pass2_ada_cuda_run.log`: the committed copy = pod copy except (1) pytest wall-clock
  `14.25s` -> `15.22s` (run-to-run timing; all 17 per-test metrics byte-identical) and
  (2) one appended summary annotation line (`kernel fixes validated: ...`). No content
  metric differs.
- `pass2_ada_cuda_run.json`: differs only in `generated_at` (15:06:06Z pod vs 15:08:45Z
  committed - the committed copy is the final post-fix capture ~2.5 min later). All 17
  test entries + summary identical.

## 7. Degradation story (gated paths degrade cleanly, never crash)

- OLD pod gate (`reports/pod_env.log`, 65.109.75.15, root): `GATE VERDICT: SKIPPED`,
  cause `Permission denied (publickey)` - the pre-key-fix path degraded to SKIPPED with
  the exact cause named, exactly per the plan's contract.
- NEW pod gate (`reports/pod_env_v2.log`, 216.81.200.13, ubuntu): **`GATE VERDICT: PASS`**
  - RTX 6000 Ada detected (sm_89), nvcc 12.2 >= 11.8, python3 + numpy/ml_dtypes/pytest OK,
  all checks `[PASS]`. With the gate PASS, every pod-runnable path (todos 9/11/14/15) ran
  and produced the evidence verified in section 6 - no SKIPPED, no crash, no fake number.

## 8. Verdict

All runnable paths executed and passed with captured output: local gate (lit 14/14,
ctest 158/158), MMA PTX compile (0 spills, real mma.sync), HIP regression (49/49 on the
7800 XT), dashboard MEASURED sm_89 + PROJECTED labels + failed correctness 0, docs audit
sweeps clean. The pod evidence is authentic: the same RTX 6000 Ada instance (UUID
GPU-5c10936b-d2ef-7838-45ce-354ccb8aed39, cc 8.9) produced 18/18 CUDA + 5/5 MMA golden
passes, `measured:true` real numbers (25.52/30.46/30.13 ms; 1.0/0.838/0.857x), and an
8/8-validated autotune with the injected broken variant REJECTED; 4 of 6 evidence files
are md5-identical to the pod copies and the remaining 2 differ only in wall-clock/
timestamp/annotation (metrics identical). The old SKIPPED gate record stands as the
documented degradation story; the new gate is PASS.

VERDICT: APPROVE
